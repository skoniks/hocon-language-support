# substitutions sample
service {
host = "localhost"
port=8080
base-url = "http://localhost:8080"
resolved = ${service.host}
optional = ${?service.missing}
nested {
value=${service.port}
}
}

include required(file("overrides.conf"))
